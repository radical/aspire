name: ZZ Shepherd Canary

# Temporary fixture for CI shepherd fork validation. Now valid and passing, to
# observe what the shepherd does with the tracker it filed while this workflow
# was failing.

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - name: Succeed
        run: echo "canary is green"
